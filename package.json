{
  "dependencies": { "react": "^18.2.0", "react-dom": "^18.2.0" },
  "scripts": {
    "dev": "bun x parcel src/index.html",
    "test": "bun tests/*.js",
    "build": "bun run test && bun x parcel build src/index.html",
    "setup": "bun i react-async-hook date-fns format-number"
  },
  "devDependencies": {
    "axios": "^1.6.2"
  }
}
